
/* !!! This is code generated by Prisma. Do not edit directly. !!! */
/* eslint-disable */
// biome-ignore-all lint: generated file
// @ts-nocheck 
import * as $runtime from "@prisma/client/runtime/library"

/**
 * @param text
 * @param text
 */
export const top_4_user_from_exam_leaderboard = $runtime.makeTypedQueryFactory("WITH top_3 AS (\nSELECT l.user_id, u.name, l.score, l.rank\nFROM leaderboard l\nJOIN \"User\" u ON l.user_id = u.id\nWHERE l.exam_id = $1  -- exam_id\nORDER BY l.rank ASC\nLIMIT 3\n),\nmy_rank AS (\nSELECT l.user_id, u.name, l.score, l.rank\nFROM leaderboard l\nJOIN \"User\" u ON l.user_id = u.id\nWHERE l.exam_id = $1\nAND l.user_id = $2  -- user_id\n),\nextra_user AS (\nSELECT l.user_id, u.name, l.score, l.rank\nFROM leaderboard l\nJOIN \"User\" u ON l.user_id = u.id\nWHERE l.exam_id = $1\nAND l.rank > (SELECT MAX(rank) FROM top_3)\nORDER BY l.rank ASC\nLIMIT 1\n)\nSELECT * FROM top_3\nUNION ALL\nSELECT * FROM my_rank WHERE user_id NOT IN (SELECT user_id FROM top_3)\nUNION ALL\nSELECT * FROM extra_user WHERE $2 IN (SELECT user_id FROM top_3);") as (text: string, text: string) => $runtime.TypedSql<top_4_user_from_exam_leaderboard.Parameters, top_4_user_from_exam_leaderboard.Result>

export namespace top_4_user_from_exam_leaderboard {
  export type Parameters = [text: string, text: string]
  export type Result = {
    user_id: string | null
    name: string | null
    score: number | null
    rank: number | null
  }
}
