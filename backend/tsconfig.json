{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "./src/*"
      ],
      "@repo/db/*": [
        "./src/db/*"
      ],
      "@repo/lib/*": [
        "./src/lib/*"
      ],
      "@repo/prisma/*": [
        "./prisma/generated/prisma/*"
      ] // here prisma is js file not ts so ts compiler should  not add into dist so we generated packages in dist directly
    },
    "rootDir": "." /* Specify the root folder within your source files. */,
    "outDir": "./dist" /* Specify an output folder for all emitted files. */,
    "module": "ESNext",
    "moduleResolution": "node",
    "target": "ES2023",
    "strict": true,
    "esModuleInterop": true,
    "incremental": true
  },
  "exclude": [
    "node_modules",
    "dist",
    "build",
    "coverage",
    "**/*.test.ts"
  ]
}